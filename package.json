{
  "name": "vscode-implicit-indent",
  "displayName": "implicit-indent",
  "description": "When the cursor moves to an empty line, automatically indent to the correct level.",
  "publisher": "jemc",
  "repository": "https://github.com/jemc/vscode-implicit-indent",
  "version": "1.1.1",
  "engines": {
    "vscode": "^1.37.0"
  },
  "categories": [
    "Formatters",
    "Other"
  ],
  "activationEvents": [
    "onCommand:implicit-indent.trigger"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "implicit-indent.trigger",
        "title": "Trigger Implicit Indent"
      }
    ],
    "keybindings": [
      {
        "command": "implicit-indent.trigger",
        "key": "ctrl ctrl",
        "when": "editorTextFocus && !editorReadonly && !suggestWidgetVisible && !parameterHintsVisible"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "devDependencies": {
    "@types/node": "^12.12.0",
    "@types/vscode": "^1.33.0",
    "@typescript-eslint/eslint-plugin": "^4.16.0",
    "@typescript-eslint/parser": "^4.16.0",
    "eslint": "^7.21.0",
    "typescript": "^4.2.2"
  },
  "dependencies": {
    "async-mutex": "^0.3.1"
  }
}
